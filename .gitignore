.idea
_site
.jekyll-metadata
.DS_Store

# IDE project/workspace files
*.iml
*.code-workspace

# The app no longer runs locally, so this file should not exist any more.
# Kept ignored so a leftover copy can never be committed: it holds real
# GitHub OAuth credentials.
admin-app/.env
