# Copy to admin-app/.env (gitignored) and fill in.
# Values come from the "arc42-trainings-admin-dev" GitHub OAuth app.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Generate with: openssl rand -hex 32
SESSION_KEY=
# Point at YOUR FORK while smoke-testing, so real PRs land somewhere harmless.
GITHUB_REPO=arc42/trainings.arc42.org-site
ENVIRONMENT=DEVELOPMENT
